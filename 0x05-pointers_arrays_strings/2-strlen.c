#include "main.h"
/**
 * _strlen - fucntion that returns the length of a string
 * @s: string pointer
 * Return: length of string int.
 */

int _strlen(char *s)
{
int len;
for (; *s != '\0'; s++)
{
	len += 1;
}
return (count);
}
