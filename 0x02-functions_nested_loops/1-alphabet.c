#include "main.h"
/**
 * print_alphabet - entry point
 *
 * Return: Always 0.
 */
void print_alphabet(void)
{
char alphabet = 'a';
while (alphabet <= 'z')
{
_putchar(alphabet);
c++;
}
_putchar('\n')
return (0);
}
